{
    "env": "development",

    "https": {
        "enabled": false,
        "enforce": true,
        "credentials": {
            "key": ".cert/server.key",
            "cert": ".cert/server.crt"
        }
    }
}