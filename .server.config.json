{
    "env": "development",

    "https": {
        "enabled": false,
        "enforce": true,
        "credentials": {
            "key": ".ssl/server.key",
            "cert": ".ssl/server.crt"
        }
    }
}